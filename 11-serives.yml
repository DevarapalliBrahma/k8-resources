apiVersion: v1
Kind: ReplicaSet
metadata:
  name: nginx
  labels:
    app: frontend
    tier: web
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
      tier: web
  template:
    metadat:
      labels:
        app: frontend
        tier: web
    spec:
      container:
      - name: nginx
        image: nginx



